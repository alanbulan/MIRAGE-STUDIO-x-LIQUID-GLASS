# Legacy AI Studio variables. These are not read by the current Pages proxy.
# For local Pages Functions, copy .dev.vars.example to .dev.vars and set
# CUSTOM_API_KEY there. For deployment, use the environment's Secret binding.
# Never use a NEXT_PUBLIC_ variable for a provider secret.
GEMINI_API_KEY=""
APP_URL=""
